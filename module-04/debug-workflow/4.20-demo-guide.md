# 🎬 Demo Guide — 4.20 Scenes 3–5
## สาธิต AI Workflow สำหรับ Debug (Design → Build → Test)

> **ไฟล์ที่ต้องเตรียมไว้ก่อนถ่าย**
> - `01-design-prompt.md` — Prompt ออกแบบ 5 องค์ประกอบ
> - `02-build-prompt.md` — Prompt สร้าง Instruction
> - `03-test-error-input.md` — Error message สำหรับทดสอบ
> - Claude Desktop เปิดค้างไว้ พร้อมสร้าง Project ใหม่

---

## ⚡ ภาพรวม 3 Scenes

```
Scene 3 (Design) → Scene 4 (Build) → Scene 5 (Test)
01-design-prompt    02-build-prompt    03-test-error-input
      ↓                    ↓                  ↓
ออกแบบ 5 องค์ประกอบ   สร้าง Instruction   ทดสอบกับ Error จริง
```

---

## Scene 3 — Design

**เป้าหมาย:** ออกแบบ Workflow ก่อน ไม่ใช่แค่ "ถาม AI"

| # | ทำบนหน้าจอ | พูด |
|---|-----------|-----|
| 1 | เปิด Claude Desktop → **+ New project** | — |
| 2 | Name: **"Help me debug"** → Description: "ผู้ช่วยวิเคราะห์สาเหตุของบัค และเสนอแนวทางการแก้ไข" → Create | — |
| 3 | New Conversation | — |
| 4 | Copy prompt จาก `01-design-prompt.md` → Paste → Send | "เริ่มออกแบบ 5 องค์ประกอบก่อนครับ" |
| 5 | รอ response → เลื่อนดู 5 องค์ประกอบ | — |
| 6 | **ชี้ที่ Input** | "Input ไม่ใช่แค่ Error message — ต้องมี Stack trace และโค้ดที่เกี่ยวข้องด้วย" |
| 7 | **ชี้ที่ Output** | "Output เรียงสาเหตุตามความน่าจะเป็น ไม่ใช่แค่คาดเดา" |
| 8 | **ชี้ที่ Context** | "Tech stack" |
| 9 | **ชี้ที่ Checkpoint** | "Developer ต้องเลือกวิธีแก้เอง — AI เสนอได้ แต่ตัดสินใจแทนไม่ได้" |
| 10 | **ชี้ที่ Criteria** | "ระบุสาเหตุที่ชัดเจน ..." |

---

## Scene 4 — Build

**เป้าหมาย:** สร้าง Instruction ที่ทำให้ AI วิเคราะห์ได้ตรงจุด

| # | ทำบนหน้าจอ | พูด |
|---|-----------|-----|
| 1 | **ใน conversation เดิม** | — |
| 2 | Copy prompt จาก `02-build-prompt.md` → Paste → Send | "สร้าง Instruction ครับ" |
| 3 | รอ response → **ชี้ที่ Output format** | "Format กำหนดชัดว่าต้องเรียงสาเหตุตามความน่าจะเป็น" |
| 4 | **ชี้ที่บรรทัด trade-off** | "ถ้ามีหลาย solution ต้องบอกด้วยว่าแต่ละทางมีข้อดีข้อเสียต่างกันยังไง" |
| 5 | Project Instructions → Edit → **Paste** → Save | — |
| 6 | **New Conversation** | — |

---

## Scene 5 — Test

**เป้าหมาย:** ทดสอบกับ Error จริง ตรวจ Criteria

| # | ทำบนหน้าจอ | พูด |
|---|-----------|-----|
| 1 | New Conversation ใน Project "Help me debug" | — |
| 2 | แนบ `03-test-error-input.md` หรือ Paste เนื้อหาเลย → Send | "ใส่ Error message ที่เตรียมไว้ครับ" |
| 3 | รอ response | — |
| 4 | **ชี้ที่ "สาเหตุที่ 1"** | "ระบุได้ชัดว่า useState ไม่มี initial value เป็น root cause" |
| 5 | **ชี้ที่ Solution steps** | "ขั้นตอนแก้ทำตามได้เลย ไม่ใช่แค่บอกว่า 'แก้ให้ถูก'" |
| 6 | **ชี้ที่ Checkpoint ท้าย** | "AI ย้ำอีกครั้งว่า Developer ต้องเลือกวิธีแก้เอง" |
| 7 | หยุดนิ่ง 2 วินาที | "นี่คือ Workflow ที่ทำซ้ำได้ทุกครั้งที่เจอบัค — ไม่ใช่การถามแบบสุ่ม" |

---

## ✅ Checklist ก่อนถ่าย

- [ ] Claude Desktop เปิดอยู่แล้ว
- [ ] ไฟล์ทั้ง 3 เปิดค้างไว้ใน text editor
- [ ] ทดลองรัน Design → Build → Test ครบ 1 รอบแล้ว
- [ ] ผลลัพธ์ Scene 5 ระบุ "useState ไม่มี initial value" เป็นสาเหตุหลัก
